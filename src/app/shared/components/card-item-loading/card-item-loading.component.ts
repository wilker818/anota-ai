import { Component, EventEmitter, Input, Output } from '@angular/core';
import { Item } from '../../../core/interfaces/item';

@Component({
  selector: 'card-item-loading',
  standalone: true,
  imports: [],
  templateUrl: './card-item-loading.component.html',
  styleUrl: './card-item-loading.component.scss',
})
export class CardItemLoadingComponent {}
